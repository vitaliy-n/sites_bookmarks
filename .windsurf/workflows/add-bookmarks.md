---
description: How to add new bookmarks to the project
---

1. Визначте останній ID у останньому `data*.js` файлі.
// turbo
2. Створіть новий файл `dataN.js` (наступний номер) або додайте в існуючий.
3. Формат закладки:
```js
BM.push(
{id:NEXT_ID, n:'Назва сайту', u:'https://example.com/', d:'Короткий опис українською.', c:'category-id', t:['тег1','тег2'], e:'🔗', f:1, inc:0}
);
```
4. Переконайтесь що `c:` (категорія) існує в `categories.js` → `CAT_TREE`.
5. Якщо категорії немає — додайте її в `categories.js` у відповідне місце дерева.
6. Підключіть новий data-файл в `index.html` перед `<script src="app.js">`.
// turbo
7. Перевірте що сайт завантажується без помилок у консолі.
